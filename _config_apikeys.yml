algolia:
  application_id: 'D4QF30HEJZ'
  index_name: 'docs_canvasmedical'
  max_record_size: 100000
  files_to_exclude:
    - 'release-notes/*/index.html'
    #  The documentreference page exceeds the algolia record size limit when
    #  we include code samples
    - 'api/documentreference/index.html'
  nodes_to_index: 'p,code'
