---
title: 09.09.2026
layout: productupdates
tags: bugfix data sdk
date: 2026-09-09
feed_summary: |
  • Revenue queue filtering by an insurer with no payer ID returns only its claims.
  • Lab Order specimen collection times save as entered; requisitions print both dates.
  • Notes with API or plugin commands load faster.
  • Nearly every Canvas record now carries created and modified, set from this release on.
  • Questionnaire date answers are readable through the SDK data module.
---

Today's release includes the following updates:

<span class="tag-bugfix">bugfix</span>

- In the revenue queue, filtering the claims list by an insurer that has no payer ID now returns only that insurer's claims. Previously every insurer without a payer ID was treated as one, so filtering by any of them returned all of their claims.
- Fixed an issue where Date/Time of Specimen Collection on a [Lab Order](/sdk/commands/#laborder) command did not save the time that was entered. Times saved from this release forward are correct, and the time picker now shows AM/PM.
- [Lab Order](/sdk/commands/#laborder) requisition forms now print Order Date and Time and Collection Date and Time as two separate fields. Previously the form carried only Collection Date and Time and filled it with the time the order was placed, so it never showed when the specimen was actually drawn. The label prints blank when no collection time was recorded.
- Notes holding commands created through the API or by a plugin now load faster.

<span class="tag-data">data</span>

- Canvas records now carry `created` and `modified` timestamps on nearly every resource, up from about two thirds of them, and the data views expose them for querying over the [read-only replica](/guides/audit-logging-and-telemetry/#read-only-replica-database). An integration can select only the rows that changed since its last run instead of re-reading whole tables.
  - Records that already existed are stamped with the time this release was deployed, not the time they were really created or last changed. Both timestamps are accurate from this release forward, so a first poll after the upgrade returns everything.
  - For the resources gaining timestamps here, the SDK data models that a plugin reads do not expose `created` and `modified` yet.

<span class="tag-sdk">sdk</span>

- A date answer on a questionnaire is now readable through the SDK data module. [`InterviewQuestionResponse`](/sdk/data-questionnaire/#interviewquestionresponse) exposes `response_option_date` as a real date, so a plugin can filter and compare on it with date lookups such as `__gte` and `__year`.
