---
title: 09.03.2026
layout: productupdates
tags: ui bugfix sdk
date: 2026-09-03
feed_summary: |
  • Prescribers can sign a prescription without an SPI number; only sending needs it.

  bugfix

  • Scheduling-modal free-text fields no longer grey out or drop characters.

  sdk

  • Plugins can dock a pane to a screen edge and add note body automations.
  • Data module adds the prescription change and cancel records.
  • Self-pay coverages report NOT_APPLICABLE eligibility, not a stale Failed.
---

Today's release includes the following updates:

<span class="tag-ui">ui</span>

- Prescribers can now review and sign a prescription without an SPI (Surescripts Prescriber Identifier) number on file.
  - SPI is only needed to transmit a prescription electronically through Surescripts, so it no longer blocks review and sign for prescribers who don't transmit that way, such as direct-integration or compounding workflows.
  - A prescription whose prescriber has no SPI still offers no send action. The same applies to Refill and Adjust Prescription.
  - An NPI number is still required to sign, and controlled-substance prescribing is still gated by EPCS enrollment.

<span class="tag-bugfix">bugfix</span>

- Fixed an issue in the appointment scheduling modal where free-text fields would grey out and lose the cursor mid-word while a scheduling plugin was installed, sometimes discarding characters.
  - Reason for Visit, Comments, Event Title, and any text field a plugin adds to the form were all affected.
  - These fields now stay editable while the form refreshes, and a refresh no longer writes over what has been typed.

<span class="tag-sdk">sdk</span>

- Plugins can now dock an application in a persistent pane on an edge of the screen: left, right, top, or bottom.
  - The pane stays mounted as the user moves between pages, so it keeps its state, and `on_context_change()` hands it the new page's context.
  - The user can resize it by dragging its handle or with the arrow keys, and Canvas remembers the size each pane was left at.
  - Up to two panes can share an edge, and the panes on one axis are capped at half the window.
  - See [Docked Applications](/sdk/handlers-embedded-applications/#docked-applications).
- Plugins can now add automations to the note body command list. Type `/` in a note body to see them alongside the native commands, each marked with a plug icon.
  - Build one as an [action button](/sdk/handlers-action-buttons/#note-body-automations) with the `NOTE_BODY_AUTOMATION` location.
  - Return a [BatchOriginateCommandEffect](/sdk/effect-batch-originate/) with `replace_line=True` and the commands land on the line the clinician typed the trigger on.
- Four more records are now readable through the SDK data module, covering the Surescripts change and cancel workflows:
  - [PrescriptionChangeResponse](/sdk/data-prescription-change-response/): the approve or deny recorded against a change request, with the response type, the status, and the response detail fields.
  - [PrescriptionChangeRequest](/sdk/data-prescription-change-request/): the pharmacy's incoming change request, with its medication codings and the prescription it relates to.
  - [CancelPrescription](/sdk/data-cancel-prescription/): a request to cancel a patient's prescription, with the prescription, the patient, the note, the message, and the status.
  - [CancelPrescriptionResponse](/sdk/data-cancel-prescription-response/): the response to a cancel request, linked one to one to the request that produced it.
- Fixed an issue where a self-pay coverage reported a stale `Failed` [eligibility status](/sdk/data-coverage/#eligibility-status) to plugins. A self-pay coverage now reports `NOT_APPLICABLE`, and the stale response behind it is never surfaced.
- A field a plugin adds to the appointment scheduling modal with [`editable=False`](/sdk/appointment-metadata-create-form-effect/) is now genuinely read-only. Previously the flag had no effect and the field stayed editable.
