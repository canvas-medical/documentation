---
title: 08.14.2026
layout: productupdates
tags: ui sdk
date: 2026-08-14
feed_summary: |
  ui

  • Prevents sending a controlled substance when a patient's sex at birth is Other or Unknown, and says why.
  • Lab, imaging and referral printouts accurately show patient's sex at birth

  sdk

  • PluginCommand now has one row per custom command, schema_key matching command_key.
---

Today's release includes the following updates:

<span class="tag-ui">ui</span>

- A controlled substance prescription can no longer be sent for a patient whose sex at birth isn't recorded as male or female, and the [Prescribe command](/sdk/commands/#prescribe) now says why: "eRx unavailable, patient sex at birth must be male or female". Previously the send failed downstream with nothing to point the prescriber at the cause.
- Lab Review, Imaging and Referral printouts now accurately reflect the patient's recorded sex at birth, including "Other" and "Unknown".

<span class="tag-sdk">sdk</span>

- The [PluginCommand](/sdk/data-plugin-command/#plugincommand) data model now holds one row per custom command. Rows left over from the old content-versioned keys are consolidated when this release is deployed, and `schema_key` now always matches the `command_key` declared in the plugin's manifest.